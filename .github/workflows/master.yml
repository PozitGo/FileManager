name: Publish C# Application

on:
  push:
    branches:
      - master  # ”кажите вашу основную ветку

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Restore Dependencies
        run: dotnet restore ./FileManager/FileManager.csproj

      - name: Build Application
        run: dotnet build ./FileManager/FileManager.csproj --configuration Release

      - name: Publish Application
        run: sudo dotnet publish ./FileManager/FileManager.csproj --output /root
